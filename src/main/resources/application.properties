spring.application.name=nimbusWebServer

spring.datasource.url=jdbc:postgresql://localhost:5432/nimbus_database
spring.datasource.username=root
spring.datasource.password=root

# reseta o banco todas as vezes
# spring.jpa.hibernate.ddl-auto=create

# atualiza os dados de acordo com as modificações, nem sempre lida bem com as alterações
spring.jpa.hibernate.ddl-auto=update

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jpa.properties.hibernate.show_sql = false
spring.jpa.properties.hibernate.format_sql = true

server.port=8081
api.security.token.secret=${JWT_SECRET:corithians2003}

cors.allowed-origins=https://trocar_por_outra_url,http://localhost:4200,http://localhost:3000

